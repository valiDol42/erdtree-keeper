using Xunit;

// Язык интерфейса - глобальное состояние: тесты перевода его переключают, а
// тесты операций сверяют русские строки. Параллельно эти два набора мешали бы
// друг другу, и падение зависело бы от порядка запуска.
[assembly: CollectionBehavior(DisableTestParallelization = true)]
